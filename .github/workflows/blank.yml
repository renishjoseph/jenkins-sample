name: Ampliance Import
on:
  workflow_dispatch:
    inputs:
      content_item:
        description: "Content items to import"
        required: true
        default: all
        type: choice
        options:
          - all
          - content/content-type-schemas
          - content/content-types
          - extensions
      environment:
        description: "Target environment"
        required: true
        default: dev
        type: choice
        options:
          - dev
          - qa
          - prod

jobs:
  build:
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment }}
    steps:
      - uses: actions/checkout@v4
      - name: Import to Ampliance Hub
        run: |
          echo "Running \"${{ github.event.inputs.content_item }}\" import for  ${{ github.event.inputs.environment }} environment."
          
          ITEM="${{ github.event.inputs.content_item }}"
          
          case "$ITEM" in
            all)
              echo "content/$ITEM"
              echo "content/$ITEM"
              echo "extensions"
              ;;
              
            content-type-schemas|content-types)
              echo "content/$ITEM"
              ;;
              
            extensions)
              echo "$ITEM"
              ;;
          esac
